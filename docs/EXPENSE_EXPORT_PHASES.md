# Property Expenses Export — Implementation Phases

This document has moved to **[PROPERTY_EXPORTS_PHASES.md](./PROPERTY_EXPORTS_PHASES.md)** — the roadmap now covers a generic property export pipeline (Exports tab, BullMQ worker, SSE) with expenses as the v1 resource type.
