import { formatLeaseMonthLabel as formatLeaseMonthLabelShared } from "@/packages/shared";

export function formatLeaseMonthLabel(month: string): string {
  return formatLeaseMonthLabelShared(month);
}
