{
  "name": "proxy",
  "version": "1.0.0",
  "private": true,
  "main": "dist/apps/proxy/src/server.js",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "build": "tsc",
    "start": "NODE_ENV=production node dist/apps/proxy/src/server.js",
    "test": "bun test",
    "lint": "eslint ."
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/rate-limit": "^10.3.0",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
