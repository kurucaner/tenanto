import type { FastifyInstance } from "fastify";

/** Phase 1: register `/tenant/me/*` and invite redemption handlers here. */
export const tenantLeaseRoutes = async (_server: FastifyInstance): Promise<void> => {};
