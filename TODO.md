- Virtualized list
  When we have more than 500 rows in a table, it becomes really slow on some devices.
  We want to improve that.
  We want to use virtualized lists. So that we only display a small portion of the full list.
  I couldn't find any reliable npm package.
  Is this something we can customly create? Or is there a solid package we can use?
  What should we do?
